const ROOT = '/'

const DISCOVERY = '/discovery'

const VIDEOS = '/videos'

const DEFAULT_ROUTE: string = DISCOVERY

const ROUTES = {
  ROOT,
  DISCOVERY,
  VIDEOS,
  DEFAULT_ROUTE
}


export default ROUTES;