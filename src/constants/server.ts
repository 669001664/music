// const LOCAL_ORIGIN = 'http://localhost'
const REMOTE_ORIGIN = 'http://localhost'

export const PORT = 3000

export const SERVER = `${REMOTE_ORIGIN}:3000/`
export const GRAPHQL_SERVER = `${REMOTE_ORIGIN}/graphql`
// __LOCALHOST__ ? `${LOCAL_ORIGIN}:${PORT}/api` : 
// __LOCALHOST__ ? `${LOCAL_ORIGIN}:${PORT}/graphql` :